// Package components holds reusable, self-contained UI widgets shared across
// tabs and modals (for example the cluster sidebar). Components render from
// data passed in by their parent and never fetch.
package components
